/* String Operations */ 

// Function Prototypes

/*
String Compare: Will compare the two strings in the parameters.
If the two strings are exactly the same, the function will return true, 
if not it will return 
*/
bool strcmp (char* stringOne, char* stringTwo);

/*
String Concatenation: Will add a space to the first parameter 
then add the second parameter to the end. Both parameters being strings.
*/
void strcat (char* firstString, char* secondString);

/*
String Copy: Will copy the string in the second parameter to
the first parameter.
*/
void strcpy (char* destination, const char* source);

/*
String Length: Will return the integer value of the length of the string in
the parameter; not including the null character.
*/
int strlen (char* string);
